---
// SkillsMarquee.astro - Infinite marquee bands (Figma: "Skills (Infinite Marquee)" #9:2)
const bandGrey = ['Svelte', 'Next.js', 'Vue.js', 'Node.js'];
const bandLime = ['PostgreSQL', 'MongoDB', 'Redis', 'Flutter'];
---

<section id="skills" class="skills-section reveal-scroll">
  <div class="skills-header">
    <div class="skills-title-wrap">
      <span class="header-highlight" aria-hidden="true"></span>
      <h2 class="skills-title">My Tools & Skills</h2>
    </div>
    <svg class="skills-scribble" width="913" height="16" viewBox="0 0 913 16" fill="none" aria-hidden="true">
      <path d="M3 9 Q 150 1, 300 8 T 600 7 T 910 6" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" fill="none" />
    </svg>
    <p class="skills-subtitle">
      A look at the tools I use every day to bring ideas to life,<br class="desktop-only" />
      traced from the initial sketch to the final implementation.
    </p>
  </div>

  <div class="marquee-stack">
    <div class="marquee-band band-grey" aria-hidden="false">
      <div class="marquee-track track-left">
        <div class="marquee-items">
          {bandGrey.map((word) => (
            <span class="marquee-item">
              <span class="marquee-word">{word}</span>
              <span class="marquee-dot dot-lime">●</span>
            </span>
          ))}
        </div>
        <div class="marquee-items" aria-hidden="true">
          {bandGrey.map((word) => (
            <span class="marquee-item">
              <span class="marquee-word">{word}</span>
              <span class="marquee-dot dot-lime">●</span>
            </span>
          ))}
        </div>
      </div>
    </div>

    <div class="marquee-band band-lime">
      <div class="marquee-track track-right">
        <div class="marquee-items">
          {bandLime.map((word) => (
            <span class="marquee-item">
              <span class="marquee-word">{word}</span>
              <span class="marquee-dot dot-black">●</span>
            </span>
          ))}
        </div>
        <div class="marquee-items" aria-hidden="true">
          {bandLime.map((word) => (
            <span class="marquee-item">
              <span class="marquee-word">{word}</span>
              <span class="marquee-dot dot-black">●</span>
            </span>
          ))}
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .skills-section {
    padding: 40px 0;
  }

  @media (min-width: 768px) {
    .skills-section {
      padding: 120px 0;
    }
  }

  .skills-header {
    margin-bottom: 24px;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  @media (min-width: 768px) {
    .skills-header {
      margin-bottom: 64px;
      align-items: flex-start;
      text-align: left;
    }
  }

  .skills-title-wrap {
    position: relative;
    display: inline-block;
  }

  .header-highlight {
    position: absolute;
    bottom: 2px;
    left: 0;
    width: 100%;
    height: 12px;
    background: rgba(193, 241, 0, 0.5);
    border-radius: 2px;
    pointer-events: none;
  }

  .skills-title {
    font-family: var(--font-headline);
    font-size: 1.75rem;
    font-weight: 800;
    line-height: 1.2;
    letter-spacing: -0.02em;
    color: var(--primary);
    position: relative;
    z-index: 1;
    text-align: center;
  }

  @media (min-width: 768px) {
    .skills-title {
      font-size: 4rem;
      text-align: left;
    }
  }

  .skills-scribble {
    display: block;
    margin-top: 8px;
    max-width: 100%;
  }

  .skills-subtitle {
    font-family: var(--font-body);
    font-size: 0.875rem;
    line-height: 1.5;
    color: var(--on-surface-variant);
    margin-top: 12px;
    max-width: 672px;
    text-align: center;
  }

  @media (min-width: 768px) {
    .skills-subtitle {
      font-size: 1.25rem;
      line-height: 32px;
      margin-top: 24px;
      text-align: left;
    }
  }

  .marquee-stack {
    display: flex;
    flex-direction: column;
    overflow: hidden;
    margin: 0 calc(-1 * var(--page-gutter));
  }

  .marquee-band {
    padding: 16px 0;
    overflow: hidden;
  }

  @media (min-width: 768px) {
    .marquee-band {
      padding: 48px 0;
    }
  }

  .band-grey {
    background: var(--surface-container);
  }

  .band-lime {
    background: rgba(193, 241, 0, 0.2);
  }

  .marquee-track {
    display: flex;
    width: max-content;
    align-items: center;
    gap: 16px;
  }

  @media (min-width: 768px) {
    .marquee-track {
      gap: 32px;
    }
  }

  .track-left {
    animation: marquee-left 30s linear infinite;
  }

  .track-right {
    animation: marquee-right 30s linear infinite;
  }

  @keyframes marquee-left {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }

  @keyframes marquee-right {
    0% { transform: translateX(-50%); }
    100% { transform: translateX(0); }
  }

  .marquee-items {
    display: inline-flex;
    align-items: center;
    gap: 16px;
  }

  @media (min-width: 768px) {
    .marquee-items {
      gap: 32px;
    }
  }

  .marquee-item {
    display: inline-flex;
    align-items: center;
    gap: 16px;
  }

  @media (min-width: 768px) {
    .marquee-item {
      gap: 32px;
    }
  }

  .marquee-word {
    font-family: var(--font-headline);
    font-size: 1.75rem;
    font-weight: 800;
    line-height: 1.2;
    letter-spacing: -0.05em;
    color: var(--primary);
    white-space: nowrap;
  }

  @media (min-width: 768px) {
    .marquee-word {
      font-size: 4rem;
    }
  }

  .marquee-dot {
    font-family: 'Liberation Serif', 'Times New Roman', serif;
    font-weight: 800;
    font-size: 1.5rem;
    line-height: 1;
  }

  @media (min-width: 768px) {
    .marquee-dot {
      font-size: 3.375rem;
    }
  }

  .dot-lime {
    color: var(--lime-accent);
  }

  .dot-black {
    color: var(--primary);
  }

  .desktop-only {
    display: none;
  }

  @media (min-width: 768px) {
    .desktop-only {
      display: inline;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .track-left,
    .track-right {
      animation: none;
    }
  }
</style>
